// Include headers for OS-specific functions, standard library, standard I/O, and string manipulation.
// #include "peachos.h"
// #include "stdlib.h"
// #include "stdio.h"
// #include "string.h"

// Main function, the entry point of the program.
// argc: argument count
// argv: argument vector (array of strings)
// int main(int argc, char** argv)
// {
    // Start an infinite loop to keep the program running.
    // while(1)
    // {
        // Print the first argument, which is the program's name.
        // print(argv[0]);

        // A simple delay loop to prevent the program from running too fast and consuming all CPU.
        // for (int i = 0; i < 1000000; i++)
        // {
            // No operation, just a delay.
        // }
    // }

    // This part is unreachable due to the infinite loop, but it's good practice to have a return statement.
    // return 0;
// }