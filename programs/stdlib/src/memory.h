// Header guard to prevent multiple inclusions of this file.
// #ifndef PEACHOS_MEMORY_H
// #define PEACHOS_MEMORY_H

// Include the standard definition file for types like size_t.
// #include <stddef.h>

// Declaration for the memset function, which sets a block of memory to a specific value.
// void* memset(void* ptr, int c, size_t size);

// Declaration for the memcmp function, which compares two blocks of memory.
// int memcmp(void* s1, void* s2, int count);

// Declaration for the memcpy function, which copies a block of memory.
// void* memcpy(void* dest, void* src, int len);

// End of header guard.
// #endif