/**
 * @file stdlib.h
 * @brief Declares standard utility functions for user-space programs, including memory allocation and integer-to-string conversion.
 */

// // Header guard
// #ifndef PEACHOS_STDLIB_H
// #define PEACHOS_STDLIB_H

// // Include for size_t type definition.
// #include <stddef.h>

// // Allocates a block of memory of a specified size from the process's heap.
// // Returns a pointer to the beginning of the block, or NULL if allocation fails.
// void* malloc(size_t size);

// // Deallocates a previously allocated block of memory.
// void free(void* ptr);

// // Converts an integer to a null-terminated string.
// char* itoa(int i);

// #endif