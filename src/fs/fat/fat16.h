// Header guard to prevent multiple inclusions.
// #ifndef FAT16_H
// #define FAT16_H

// Include the main file system header for the `filesystem` structure.
// #include "file.h"

// Initializes the FAT16 filesystem driver.
// This function creates and returns a `filesystem` structure populated with
// pointers to the FAT16 implementation of open, read, seek, etc.
// struct filesystem* fat16_init();

// End of header guard.
// #endif