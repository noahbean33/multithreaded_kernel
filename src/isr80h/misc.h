// Header guard to prevent multiple inclusions.
// #ifndef ISR80H_MISC_H
// #define ISR80H_MISC_H

// Forward declaration of the interrupt_frame struct.
// struct interrupt_frame;

// Declares the handler for system call 0 (sum).
// This is a simple test function that takes arguments from the interrupt frame stack
// and returns their sum.
// void* isr80h_command0_sum(struct interrupt_frame* frame);

// #endif